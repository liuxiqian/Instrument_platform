'use strict';

describe('Controller: SystemMenuAddcontrollerCtrl', function () {

  // load the controller's module
  beforeEach(module('webappApp'));

  // var SystemMenuAddcontrollerCtrl,
  //   scope;

  // // Initialize the controller and a mock scope
  // beforeEach(inject(function ($controller, $rootScope) {
  //   scope = $rootScope.$new();
  //   SystemMenuAddcontrollerCtrl = $controller('SystemMenuAddcontrollerCtrl', {
  //     $scope: scope
  //     // place here mocked dependencies
  //   });
  // }));

  // it('should attach a list of awesomeThings to the scope', function () {
  //   expect(SystemMenuAddcontrollerCtrl.awesomeThings.length).toBe(3);
  // });
});

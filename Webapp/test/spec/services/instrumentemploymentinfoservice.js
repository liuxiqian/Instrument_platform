'use strict';

describe('Service: InstrumentEmploymentInfo', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var InstrumentEmploymentInfo;
  beforeEach(inject(function (_InstrumentEmploymentInfo_) {
    InstrumentEmploymentInfo = _InstrumentEmploymentInfo_;
  }));

  // it('should do something', function () {
  //   expect(!!InstrumentEmploymentInfo).toBe(true);
  // });

});

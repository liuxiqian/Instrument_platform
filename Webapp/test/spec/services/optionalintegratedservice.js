'use strict';

describe('Service: OptionalIntegratedService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var OptionalIntegratedService;
  beforeEach(inject(function (_OptionalIntegratedService_) {
    OptionalIntegratedService = _OptionalIntegratedService_;
  }));

  // it('should do something', function () {
  //   //expect(!!OptionalIntegratedService).toBe(true);
  // });

});

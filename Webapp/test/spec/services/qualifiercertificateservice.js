'use strict';

describe('Service: QualifierCertificateService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var QualifierCertificateService;
  beforeEach(inject(function (_QualifierCertificateService_) {
    QualifierCertificateService = _QualifierCertificateService_;
  }));

  // it('should do something', function () {
  //   expect(!!QualifierCertificateService).toBe(true);
  // });

});

'use strict';

describe('Service: RoleRolefileService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var RoleRolefileService;
  beforeEach(inject(function (_RoleRolefileService_) {
    RoleRolefileService = _RoleRolefileService_;
  }));

  // it('should do something', function () {
  //   expect(!!RoleRolefileService).toBe(true);
  // });

});

'use strict';

describe('Service: UserUserfileService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var UserUserfileService;
  beforeEach(inject(function (_UserUserfileService_) {
    UserUserfileService = _UserUserfileService_;
  }));

  // it('should do something', function () {
  //   expect(!!UserUserfileService).toBe(true);
  // });

});

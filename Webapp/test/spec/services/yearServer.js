'use strict';

describe('Service: yearServer', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var yearServer;
  beforeEach(inject(function (_yearServer_) {
    yearServer = _yearServer_;
  }));

  // it('should do something', function () {
  //   //expect(!!yearServer).toBe(true);
  // });

});

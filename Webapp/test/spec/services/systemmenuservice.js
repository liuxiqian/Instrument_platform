'use strict';

describe('Service: systemmenuservice', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var systemmenuservice;
  beforeEach(inject(function (_systemmenuservice_) {
    systemmenuservice = _systemmenuservice_;
  }));

  // it('should do something', function () {
  //   expect(!!systemmenuservice).toBe(true);
  // });

});

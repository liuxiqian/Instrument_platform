'use strict';

describe('Service: ApplianceServer', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var ApplianceServer;
  beforeEach(inject(function (_ApplianceServer_) {
    ApplianceServer = _ApplianceServer_;
  }));

  // it('should do something', function () {
  //   //expect(!!ApplianceServer).toBe(true);
  // });

});

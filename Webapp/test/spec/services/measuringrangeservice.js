'use strict';

describe('Service: MeasuringRangeService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var MeasuringRangeService;
  beforeEach(inject(function (_MeasuringRangeService_) {
    MeasuringRangeService = _MeasuringRangeService_;
  }));

  // it('should do something', function () {
  //   expect(!!MeasuringRangeService).toBe(true);
  // });

});

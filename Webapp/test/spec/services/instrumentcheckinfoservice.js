'use strict';

describe('Service: InstrumentCheckInfoService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var InstrumentCheckInfoService;
  beforeEach(inject(function (_InstrumentCheckInfoService_) {
    InstrumentCheckInfoService = _InstrumentCheckInfoService_;
  }));

  // it('should do something', function () {
  //   expect(!!InstrumentCheckInfoService).toBe(true);
  // });

});

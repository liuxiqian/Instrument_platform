'use strict';

describe('Service: RegisterEnterpriseService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var RegisterEnterpriseService;
  beforeEach(inject(function (_RegisterEnterpriseService_) {
    RegisterEnterpriseService = _RegisterEnterpriseService_;
  }));

  // it('should do something', function () {
  //   expect(!!RegisterEnterpriseService).toBe(true);
  // });

});

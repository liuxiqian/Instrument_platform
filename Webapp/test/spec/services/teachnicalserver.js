'use strict';

describe('Service: TeachnicalServer', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var TeachnicalServer;
  beforeEach(inject(function (_TeachnicalServer_) {
    TeachnicalServer = _TeachnicalServer_;
  }));

  // it('should do something', function () {
  //   //expect(!!TeachnicalServer).toBe(true);
  // });

});

'use strict';

describe('Service: standardFileService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var standardFileService;
  beforeEach(inject(function (_standardFileService_) {
    standardFileService = _standardFileService_;
  }));

  // it('should do something', function () {
  //   //expect(!!standardFileService).toBe(true);
  // });

});

'use strict';

describe('Service: districtmanageservice', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var districtmanageservice;
  beforeEach(inject(function (_districtmanageservice_) {
    districtmanageservice = _districtmanageservice_;
  }));

  // it('should do something', function () {
  //   expect(!!districtmanageservice).toBe(true);
  // });

});

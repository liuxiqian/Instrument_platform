'use strict';

describe('Service: districtService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var districtService;
  beforeEach(inject(function (_districtService_) {
    districtService = _districtService_;
  }));

  // it('should do something', function () {
  //   //expect(!!districtService).toBe(true);
  // });

});

'use strict';

describe('Service: departmentDirectiveService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var departmentDirectiveService;
  beforeEach(inject(function (_departmentDirectiveService_) {
    departmentDirectiveService = _departmentDirectiveService_;
  }));

  // it('should do something', function () {
  //   expect(!!departmentDirectiveService).toBe(true);
  // });

});

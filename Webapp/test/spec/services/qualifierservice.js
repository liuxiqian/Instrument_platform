'use strict';

describe('Service: QualifierService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var QualifierService;
  beforeEach(inject(function (_QualifierService_) {
    QualifierService = _QualifierService_;
  }));

  // it('should do something', function () {
  //   expect(!!QualifierService).toBe(true);
  // });

});

'use strict';

describe('Service: PersonalFileService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var PersonalFileService;
  beforeEach(inject(function (_PersonalFileService_) {
    PersonalFileService = _PersonalFileService_;
  }));

  // it('should do something', function () {
  //   expect(!!PersonalFileService).toBe(true);
  // });

});

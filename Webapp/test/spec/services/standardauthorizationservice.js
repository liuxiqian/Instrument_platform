'use strict';

describe('Service: StandardAuthorizationService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var StandardAuthorizationService;
  beforeEach(inject(function (_StandardAuthorizationService_) {
    StandardAuthorizationService = _StandardAuthorizationService_;
  }));

  // it('should do something', function () {
  //   //expect(!!StandardAuthorizationService).toBe(true);
  // });

});

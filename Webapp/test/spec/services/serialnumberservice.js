'use strict';

describe('Service: serialNumberService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var serialNumberService;
  beforeEach(inject(function (_serialNumberService_) {
    serialNumberService = _serialNumberService_;
  }));

  // it('should do something', function () {
  //   expect(!!serialNumberService).toBe(true);
  // });

});

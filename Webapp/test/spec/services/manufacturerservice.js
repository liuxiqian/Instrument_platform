'use strict';

describe('Service: ManufacturerService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var ManufacturerService;
  beforeEach(inject(function (_ManufacturerService_) {
    ManufacturerService = _ManufacturerService_;
  }));

  // it('should do something', function () {
  //   expect(!!ManufacturerService).toBe(true);
  // });

});

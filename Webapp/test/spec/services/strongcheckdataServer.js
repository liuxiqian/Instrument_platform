'use strict';

describe('Service: strongcheckdataServer', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var strongcheckdataServer;
  beforeEach(inject(function (_strongcheckdataServer_) {
    strongcheckdataServer = _strongcheckdataServer_;
  }));

  // it('should do something', function () {
  //   //expect(!!strongcheckdataServer).toBe(true);
  // });

});

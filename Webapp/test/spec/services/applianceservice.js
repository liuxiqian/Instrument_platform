'use strict';

describe('Service: ApplianceService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var ApplianceService;
  beforeEach(inject(function (_ApplianceService_) {
      ApplianceService = _ApplianceService_;
  }));

  // it('should do something', function () {
  //   expect(!!ApplianceService).toBe(true);
  // });

});

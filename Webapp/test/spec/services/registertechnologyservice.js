'use strict';

describe('Service: RegisterTechnologyService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var RegisterTechnologyService;
  beforeEach(inject(function (_RegisterTechnologyService_) {
    RegisterTechnologyService = _RegisterTechnologyService_;
  }));

  // it('should do something', function () {
  //   expect(!!RegisterTechnologyService).toBe(true);
  // });

});

'use strict';

describe('Service: MandatoryCheckdetailService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var MandatoryCheckdetailService;
  beforeEach(inject(function (_MandatoryCheckdetailService_) {
    MandatoryCheckdetailService = _MandatoryCheckdetailService_;
  }));

  // it('should do something', function () {
  //   expect(!!MandatoryCheckdetailService).toBe(true);
  // });

});

'use strict';

describe('Service: PersonnelFileService', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var PersonnelFileService;
  beforeEach(inject(function (_PersonnelFileService_) {
    PersonnelFileService = _PersonnelFileService_;
  }));

  // it('should do something', function () {
  //   //expect(!!PersonnelFileService).toBe(true);
  // });

});

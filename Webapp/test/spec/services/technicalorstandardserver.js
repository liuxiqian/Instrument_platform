'use strict';

describe('Service: TechnicalOrStandardServer', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var TechnicalOrStandardServer;
  beforeEach(inject(function (_TechnicalOrStandardServer_) {
    TechnicalOrStandardServer = _TechnicalOrStandardServer_;
  }));

  // it('should do something', function () {
  //   //expect(!!TechnicalOrStandardServer).toBe(true);
  // });

});

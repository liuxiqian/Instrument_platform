'use strict';

describe('Service: registerMeasureDeviceservice', function () {

  // load the service's module
  beforeEach(module('webappApp'));

  // instantiate service
  var registerMeasureDeviceservice;
  beforeEach(inject(function (_registerMeasureDeviceservice_) {
    registerMeasureDeviceservice = _registerMeasureDeviceservice_;
  }));

  // it('should do something', function () {
  //   expect(!!registerMeasureDeviceservice).toBe(true);
  // });

});

'use strict';

describe('Service: OptionalCheckdetailService', function () {
	
	// load the service's module
	beforeEach(module('webappApp'));
	
	// instantiate service
	var OptionalCheckdetailService;
	beforeEach(inject(function (_OptionalCheckdetailService_) {
		OptionalCheckdetailService = _OptionalCheckdetailService_;
	}));
	
	it('should do something', function () {
		// 增加一条断言
		expect(true).toBe(true);
	});
	
});
